<idea-plugin>
    <id>com.anabulatovic.UniqueCompletion</id>

    <name>Unique Completion</name>

    <vendor url="https://www.github.com/anabulatovic">Ana Bulatovic</vendor>

    <description><![CDATA[
        A productivity tool that cleans up IntelliSense suggestions.
        It hides variables from the autocomplete list if they have already been added to the current
        <code>List.of()</code> or <code>Set.of()</code> method call.
    ]]></description>

    <depends>com.intellij.modules.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <completion.contributor
            language="JAVA"
            implementationClass="filter.UniqueCompletionContributor"/>
    </extensions>
</idea-plugin>